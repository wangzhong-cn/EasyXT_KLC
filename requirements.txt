# 核心依赖
pandas>=1.3.0
numpy>=1.21.0
matplotlib>=3.5.0
requests>=2.25.0
psutil>=5.9.0

# 数据存储
pyarrow>=10.0.0  # Parquet文件支持（数据管理需要）
fastparquet>=0.8.0  # Parquet备用引擎（可选）

# 数据源（可选）
qstock>=1.0.0
akshare>=1.9.0

# 监控/服务（可选）
fastapi>=0.103.0
uvicorn>=0.23.0
pydantic>=1.10.0

# GUI依赖（可选）
PyQt5>=5.15.0
pillow>=8.0.0

# 回测（可选）
backtrader>=1.9.78

# 开发依赖（可选）
pytest>=6.0.0
black>=21.0.0
flake8>=3.9.0

# ============================================================================
# EasyXT 量化交易平台 — 依赖清单
# 验证环境版本已维护在注释中（2025-06 实测）
# ============================================================================

# ── 核心数值计算 ────────────────────────────────────────────────────────────
pandas>=2.0.0,<3          # 实测 2.3.3
numpy>=1.21.0,<3          # 实测 2.3.5
matplotlib>=3.5.0
requests>=2.25.0
psutil>=5.9.0
msgpack>=1.0.7
websockets>=10.0           # native chart adapter WebSocket bridge
schedule>=1.2.0            # auto_data_updater 定时补数/审计调度

# ── 数据存储 ────────────────────────────────────────────────────────────────
duckdb>=1.0.0              # 实测 1.4.2
pyarrow>=10.0.0            # 实测 22.0.0；Parquet 支持
fastparquet>=0.8.0         # Parquet 备用引擎（可选）

# ── 数据源（可选）──────────────────────────────────────────────────────────
qstock>=1.0.0
akshare>=1.9.0
chinesecalendar>=1.10.0    # A股权威节假日库（TradingCalendar 使用）

# ── API 服务 ────────────────────────────────────────────────────────────────
fastapi>=0.100.0           # 实测 0.121.1
uvicorn>=0.23.0
pydantic>=2.0.0            # 实测 2.12.4；v1 已不兼容，请勿降级

# ── 可观测性 ────────────────────────────────────────────────────────────────
prometheus-client>=0.19.0  # /metrics Prometheus 暴露端点

# ── GUI 依赖（可选）────────────────────────────────────────────────────────
PyQt5>=5.15.0
pillow>=8.0.0

# ── 回测（可选）────────────────────────────────────────────────────────────
backtrader>=1.9.78

# ── 开发 / CI 依赖 ──────────────────────────────────────────────────────────
pytest>=6.0.0
pytest-qt>=4.2.0
black>=21.0.0
flake8>=3.9.0
ruff>=0.1.0
mypy>=1.0.0
pre-commit>=3.0.0
coverage>=7.0.0
